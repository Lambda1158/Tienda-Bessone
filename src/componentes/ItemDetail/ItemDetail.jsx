import React from 'react'
import ItemCount from '../ItemCount/ItemCount'

const ItemDetail = ({id}) => {
    

    return (
            <div>
              <h2>ID de la Pocion: {id}</h2>
            </div>
    );
};

export default ItemDetail;
